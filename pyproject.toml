[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zilver"
version = "0.5.0"
description = "Open quantum simulation network for Apple Silicon"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
keywords = ["quantum computing", "simulation", "apple silicon", "mlx", "distributed", "network", "barren plateau", "variational quantum algorithms"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: System :: Distributed Computing",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    # MLX ships wheels for Apple silicon only, so an unconditional requirement
    # made `pip install zilver` fail at resolution on every other platform --
    # which confined the node fleet to Macs. The marker makes it optional;
    # zilver/_array.py supplies a numpy-backed mlx.core when it is absent.
    "mlx>=0.18.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "numpy>=1.24.0",
]

[project.urls]
Homepage  = "https://github.com/sirius-quantum/zilver"
Repository = "https://github.com/sirius-quantum/zilver"
Issues    = "https://github.com/sirius-quantum/zilver/issues"
Manifesto = "https://github.com/sirius-quantum/zilver/blob/main/MANIFESTO.md"

[project.optional-dependencies]
dev     = ["pytest>=7.4", "pytest-benchmark>=4.0"]
qiskit  = ["qiskit-aer>=0.12", "qiskit>=0.45"]
accel   = ["numba>=0.59"]
network = ["fastapi>=0.135.2", "starlette>=0.49.1", "uvicorn[standard]>=0.27", "httpx>=0.27", "slowapi>=0.1.9", "cryptography>=42.0", "rich>=13.0", "pyobjc-framework-Security>=10.0; sys_platform == 'darwin'"]

[project.scripts]
zilver-node     = "zilver.cli:main"
zilver-registry = "zilver.cli:main_registry"

[tool.hatch.build.targets.wheel]
packages = ["src/zilver"]

[tool.pytest.ini_options]
testpaths = ["tests"]
